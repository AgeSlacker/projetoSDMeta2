<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE struts PUBLIC
        "-//Apache Software Foundation//DTD Struts Configuration 2.5//EN"
        "http://struts.apache.org/dtds/struts-2.5.dtd">

<struts>
    <constant name="struts.ui.theme" value="simple"></constant>
    <package name="webserver" extends="struts-default">
        <interceptors>
            <interceptor name="requireLogin" class="webserver.interceptors.LoginInterceptor"></interceptor>
            <interceptor-stack name="appDefault">
                <interceptor-ref name="defaultStack">
                    <param name="exception.logEnabled">true</param>
                    <param name="exception.logLevel">ERROR</param>
                    <param name="params.excludeParams">admin,Admin,setAdmin</param>
                </interceptor-ref>
            </interceptor-stack>
        </interceptors>
        <default-interceptor-ref name="appDefault"></default-interceptor-ref>
        <default-action-ref name="index" />
        <action name="index">
            <result>/index.jsp</result>
        </action>
        <action name="searchResults" class="webserver.action.SearchAction">
            <result name="success">/searchResults.jsp</result>
            <result name="error">/index.jsp</result>
            <result name="login">/login.jsp</result>
        </action>

        <action name="login" class="webserver.action.LoginAction">
            <result name="success">/index.jsp</result>
            <result name="error">/login.jsp</result>
        </action>
    </package>
</struts>